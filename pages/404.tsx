import React from 'react';

import NotFound from 'components/NotFound';

const NotFoundPage = () => {
    return (
        <main>
            <NotFound/>
        </main>
    );
};

export default NotFoundPage;